version: 0.0
os: linux
files:
  - source: .
    destination: /home/ec2-user/codedeploy/

hooks:
  BeforeInstall:
    - location: cleanup.sh
      timeout: 180
      runas: root
  ApplicationStart:
    - location: deploy.sh
      timeout: 300
      runas: root
